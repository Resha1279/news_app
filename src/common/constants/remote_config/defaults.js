import {VERSION_CODE} from '../index';

export const CONSTANTS = {
  test: true,
  feeds_item_ads_interval: 3,
  interstitial_ads_interval: 4,
  interstitial_video_ads_interval: 6,
  view_pager_length: 5,
  update_available_hide_time: 100,
  license_info: 'Text from remote config',
  about_us: 'About us Text from remote config',
  version_code: VERSION_CODE,
};
